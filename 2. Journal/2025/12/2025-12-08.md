---
tags: dailies  
date: 2025-12-08
create: 2025-12-08-Mon
time: 11:49
---
## 每日精華

https://www.freecodecamp.org/learn/daily-coding-challenge/2025-12-07
```js
function compressString(sentence) {
	const words = Array.from(sentence.matchAll(/\w+/g), x => x[0])
	let count = 0
	const res = []
	for (let i = 0; i < words.length; i++) {
		count++
		if (i+1 >= words.length || words[i] !== words[i+1]) {
			res.push(words[i] + (count > 1 ? `(${count})` : ""))
			count = 0
		}
	}
	return res.join(" ")
}
compressString("yes yes yes please")
compressString("one one three and to the the the the")
compressString("route route route route route route tee tee tee tee tee tee")
```


## 完成事項