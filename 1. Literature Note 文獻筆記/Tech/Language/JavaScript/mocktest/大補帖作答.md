---
tags: []
date: 2026-05-05
time: 21:50
---

##### Q5
value, writable 和 get, set 不可同時設定。

##### Q7
undefined, null 轉變成數字和字串各會是什麼？
NaN, 0
"undefined", "null"

##### Q10
我竟然把 switch() 給的數字，當成 x 來處理。
閱讀理解需要小心。

##### Q11
`+[]` 等於 `0`

##### Q14
includes use SameValueZero
indexOf use Strict Equality

##### Q15
Set, Map 使用 SameValueZero (includes)

##### Q16
Object.seal 允許修改已有的屬性值，但是不允許新增，也不允許 reconfigure

##### Q17
freeze 是 shallow 的

##### Q19
- class body 本身是 strict mode 的
- 沒有用 new Class 的話，會有 TypeError

##### Q30
|情況|能否重新賦值|能否改內部屬性|
|---|---|---|
|Primitive import|❌|N/A|
|Object import|❌|✅|
|export 端自己改|✅|✅|

##### Q32
`export default const x = 1` 是錯誤用法，不可以包含 let, const, var。 default 後面接的要是一個表達式。
`export const inc = ()=> {n++;}`是可以的，叫做「具名匯出」。

##### Q32
Object 不是 iterable 的。

##### Q35
`function*() {}` 不會馬上執行，只會回傳 generator 物件。