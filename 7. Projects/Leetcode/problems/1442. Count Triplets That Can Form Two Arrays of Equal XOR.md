---
tags:
  - prefix-hash
date: 2026-01-18
time: 23:15
cards-deck: leetcode
---

# [1442. Count Triplets That Can Form Two Arrays of Equal XOR](https://leetcode.com/problems/count-triplets-that-can-form-two-arrays-of-equal-xor/) #card
^1768749395274

這題有成功解出來，我感到安心。我目前連續練習這種題型 4, 5 題了。

這題算是有點變化球。有兩個方面：

###### 1. 看似很複雜的計算過程，但其實因為 XOR，我們要求的只是 `XOR(nums[i..k]) === 0` 。 j 可以是 `[i+1..k]` 之間的任意值

###### 2. 承上，我們存在 prefix map 的不是單純的數量，而是 prefix 結束的位置，因為我們需要 target 的起始位置，才能計算 j 可以出現的位置在哪。

```
[]
這段 XOR 是 0，
```